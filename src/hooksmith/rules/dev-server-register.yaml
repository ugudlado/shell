rules:
  # Track dev server processes started via bash (runs in background)
  - name: dev-server-register
    on: PostToolUse Bash
    run: ~/.config/hooksmith/scripts/dev-server-register.sh
    context: true
