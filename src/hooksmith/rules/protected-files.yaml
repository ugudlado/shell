rules:
  # Deny edits to lock files, plugin manifests, and other protected files
  - name: protected-files
    on: PreToolUse Write|Edit
    run: ~/.config/hooksmith/scripts/protected-files.sh
    ask: true
