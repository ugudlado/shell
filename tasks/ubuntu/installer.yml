- name: Update Apt
  become: true
  apt:
    force_apt_get: true
    update_cache: true
    state: present

- name: Install packages with apt
  ansible.builtin.apt:
    name:
      - git
      - tmux
      - zsh
      - fzf
      - direnv
      - stow
      - docker
    state: present
