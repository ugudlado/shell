node_modules/
.pnpm-store
.ssh/
.oh-my-zsh/
.bash_logout
.bash_history
.motd_shown
.sudo*
.viminfo*
.wget-*
.zc*
.z
.zsh_history
.zshrc.pre-oh-my-zsh*
.vscode-server/*
*.log
.cache
.npm
.DS_Store*

.security-scans
.envrc-personal
.env.local

# Local memory files (project-specific, not templates in src/)
/memory.json

# Spec CLI-generated skills (regenerated by `spec init --tools claude`)
src/claude/skills/spec-*/

# Per-change workflow state (ephemeral, not an artifact)
spec/changes/*/state.yaml

**/node_modules
tools/algoviz/

# Cursor generated/local files
.cursor/settings.json

# Local MCP registry (copy from tools/mcp-registry.example.yaml)
tools/mcp-registry.yaml

icon.svg
src/hooksmith/.map.json
