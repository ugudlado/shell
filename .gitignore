node_modules/
.pnpm-store
.ssh/
.oh-my-zsh/
.bash_logout
.bash_history
.motd_shown
.sudo*
.viminfo*
.wget-*
.zc*
.z
.zsh_history
.zshrc.pre-oh-my-zsh*
.vscode-server/*
*.log
.cache
.npm
.DS_Store*

.security-scans
.envrc-personal
.env.local

# Local memory files (project-specific, not templates in src/)
/memory.json

# OpenSpec CLI-generated skills (regenerated by `openspec init --tools claude`)
src/claude/skills/openspec-*/

# Per-change workflow state (ephemeral, not an artifact)
openspec/changes/*/state.yaml

**/node_modules
tools/algoviz/

# Flux source build (cloned locally, not versioned)
tools/flux-src/

# Flux local data (syncs via flux-data branch, global store at ~/code/shell/.flux/)
.flux/

# Cursor generated/local files
.cursor/settings.json

# Local MCP registry (copy from tools/mcp-registry.example.yaml)
tools/mcp-registry.yaml
